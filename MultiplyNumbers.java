public class MultiplyNumbers {
    public static int multiplyNumbers(int[] array, int p) {
        int pVal;
        int sum = 0;
        for(i=0,j=array.length-1;;i++) {
            if(i==p) {
                pVal = array[i];
            }
            else if((i = array.length -i -1)/2 
            else {
                sum += array[i] * array[array.length - i - 1];
            } 
            i
        }
        return sum - p;
    } 
}
